apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# secret.yaml is a template with a placeholder key and is deliberately NOT
# listed here: create the real Secret out of band (see the comments in that
# file) so a credential never lands in git.
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
